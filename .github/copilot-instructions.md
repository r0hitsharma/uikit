# Copilot instructions

See `AGENTS.md` for the repo baseline. When reviewing any PR, stack, branch, or
diff in this repository, read `docs/REVIEWING.md` and follow it: it defines the
commit-type release semantics, the public-repo constraints, per-layer review of
stacked PRs, and the requirement that findings be proved by running them and
delivered as PR comments rather than summaries.
