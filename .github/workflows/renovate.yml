name: Renovate

on:
  schedule:
    # Weekly on Monday at 10:00 UTC (matches original dependabot schedule)
    - cron: '0 10 * * 1'
  workflow_dispatch:

# Explicit, so the job does not depend on the repository's default token
# permissions (read-only on new repositories and forks). The list is
# exhaustive: any scope left out is none. Renovate reports a failed
# commit-status POST as "Repository has changed during renovation", so a
# missing `statuses` scope aborts the whole run with no other clue.
permissions:
  contents: write
  issues: write
  pull-requests: write
  statuses: write # the renovate/stability-days check from minimumReleaseAge

concurrency:
  group: renovate
  cancel-in-progress: false

jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Self-hosted Renovate
        uses: renovatebot/github-action@79dc0ba74dc3de28db0a7aeb1d0b95d5bf5fde2a # v46.1.13
        env:
          RENOVATE_AUTODISCOVER: "false"
          RENOVATE_REPOSITORIES: ${{ github.repository }}
        with:
          configurationFile: renovate.json5
          token: ${{ secrets.GITHUB_TOKEN }}
