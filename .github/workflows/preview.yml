name: preview

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed

permissions:
  contents: write
  packages: read
  pull-requests: write

jobs:
  resolve-preview-context:
    # Only build previews on push to main or open/active PRs (not closed)
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    outputs:
      comment_pr_number: ${{ steps.context.outputs.comment_pr_number }}
      publish_event_name: ${{ steps.context.outputs.publish_event_name }}
      publish_pr_number: ${{ steps.context.outputs.publish_pr_number }}
      publish_sha: ${{ steps.context.outputs.publish_sha }}
      should_run: ${{ steps.context.outputs.should_run }}
    steps:
      - name: Resolve preview context
        id: context
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          SHA: ${{ github.sha }}
        run: |
          should_run=true

          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            publish_pr_number="$PR_NUMBER"
            comment_pr_number="$PR_NUMBER"
          fi

          echo "should_run=${should_run:-true}" >> "$GITHUB_OUTPUT"
          echo "publish_event_name=${EVENT_NAME}" >> "$GITHUB_OUTPUT"
          echo "publish_sha=${SHA}" >> "$GITHUB_OUTPUT"
          echo "publish_pr_number=${publish_pr_number:-}" >> "$GITHUB_OUTPUT"
          echo "comment_pr_number=${comment_pr_number:-}" >> "$GITHUB_OUTPUT"

  build-and-deploy:
    needs: resolve-preview-context
    if: needs.resolve-preview-context.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    # Scoped per PR (or per main) rather than one repo-wide group: GitHub only
    # keeps one job queued per concurrency group, so a single shared group
    # across every PR causes unrelated PRs to cancel each other out of the
    # queue. Cross-PR pushes to gh-pages can still race, but the publish
    # script retries against a fresh fetch on conflict, so that's safe.
    concurrency:
      group: gh-pages-preview-${{ needs.resolve-preview-context.outputs.publish_pr_number && format('pr-{0}', needs.resolve-preview-context.outputs.publish_pr_number) || 'main' }}
      cancel-in-progress: false
    steps:

      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .node-version
          cache: npm

      - name: Enable Corepack
        run: corepack enable npm

      - name: Install dependencies
        run: npm ci

      - name: Build packages
        env:
          # Enables the live MCP Connect story; unset, it renders a notice
          # instead of connecting. See packages/uikit-preview/demo-relay.
          VITE_DEMO_RELAY_URL: ${{ vars.DEMO_RELAY_URL }}
        run: npm run build

      - name: Publish preview to gh-pages
        env:
          EVENT_NAME: ${{ needs.resolve-preview-context.outputs.publish_event_name }}
          PR_NUMBER: ${{ needs.resolve-preview-context.outputs.publish_pr_number }}
          SHA: ${{ needs.resolve-preview-context.outputs.publish_sha }}
        run: bash .github/scripts/publish-preview.sh

      - name: Comment PR with preview links
        if: needs.resolve-preview-context.outputs.comment_pr_number != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ needs.resolve-preview-context.outputs.comment_pr_number }}
          PREVIEW_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/pr/${{ needs.resolve-preview-context.outputs.comment_pr_number }}/
        run: |
          set -euo pipefail

          MARKER="<!-- uikit-preview-links -->"
          BODY=$(cat <<EOF
          ${MARKER}
          Preview is ready:

          - Preview: ${PREVIEW_URL}
          EOF
          )

          EXISTING_ID=$(gh api "repos/${REPO}/issues/${PR_NUMBER}/comments" \
            --jq ".[] | select(.body | contains(\"${MARKER}\")) | .id" \
            | head -n1 || true)

          if [[ -n "${EXISTING_ID}" ]]; then
            gh api \
              "repos/${REPO}/issues/comments/${EXISTING_ID}" \
              --method PATCH \
              -f body="$BODY" >/dev/null
          else
            gh api \
              "repos/${REPO}/issues/${PR_NUMBER}/comments" \
              --method POST \
              -f body="$BODY" >/dev/null
          fi

  cleanup:
    # Run for all closed PRs (merged or not). Same group as this PR's
    # build-and-deploy so the two can't race each other, but distinct from
    # other PRs' groups so a burst of closes (e.g. merging a stack) doesn't
    # cancel each other's cleanup out of the queue.
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    concurrency:
      group: gh-pages-preview-pr-${{ github.event.pull_request.number }}
      cancel-in-progress: false
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Remove PR preview path from gh-pages
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: bash .github/scripts/cleanup-pr-preview.sh
